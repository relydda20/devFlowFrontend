# Tasks: feature-landing-page

- [x] 1. Open `pages/Landing.tsx` (or equivalent main page component).
- [x] 2. Ensure the main wrapper has `bg-[#0E1322] min-h-screen text-slate-300`.
- [x] 3. Build the Hero section. Match the exact text: "Master Your AI-Assisted Workflow".
- [x] 4. Implement the Hero CTA Button. Use `bg-[#B3C5FF] text-[#0E1322] hover:bg-[#B3C5FF]/80`. Add the monospace sub-text below it.
- [x] 5. Build the 3-column Features Grid using Shadcn `<Card>`.
- [x] 6. For each card, implement the colored Icon Box in the `CardHeader` (Blue for Churn, Purple for Context, Green for AI).
- [x] 7. Insert the exact descriptions from `spec.md` into the `CardContent`.
- [x] 8. Implement the `CardFooter` for each card using `font-mono text-xs flex justify-between`. Apply the specific status colors (`text-emerald-400` or `text-orange-400`).
- [x] 9. Create a new component `components/layout/Footer.tsx`.
- [x] 10. Build the Footer layout with a subtle `border-t border-white/10`. Add the logo, copyright text, and links using `text-slate-400 hover:text-white`.
- [x] 11. Import and mount `<Footer />` at the bottom of the main app layout or Landing page.
