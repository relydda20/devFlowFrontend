# Tasks: fix-login-hover
- [x] 1. Open `components/layout/Navbar.tsx`.
- [x] 2. Locate the Login `<Button>`.
- [x] 3. Update its `className` to explicitly define the hover state so it overrides the Shadcn defaults. 
- [x] 4. Apply these exact classes: `bg-[#B3C5FF] text-[#0E1322] hover:bg-[#B3C5FF]/80 transition-colors`.
