# Tasks: fix-docs-content

- [x] 1. Open `pages/Docs.tsx`.
- [x] 2. Completely remove the existing generic text paragraphs under the main content sections.
- [x] 3. Insert the exact text, terminal commands, and bullet points provided in `spec.md`.
- [x] 4. Format the terminal commands (e.g., `npm run compile`) using the existing dark code block UI components.
- [x] 5. Ensure the sidebar navigation links still correctly anchor to these updated sections: Installation & Setup, Running the Extension, Testing Telemetry Events, and Synchronization & Status.
