## Why

The `/docs` page is functional, but its current content is still generic and only loosely describes the product workflow. We need to tighten the documentation so it explains DevFlow in a clearer, more useful way for someone opening the page for the first time.

## What Changes

- Refresh the docs page copy so it better explains DevFlow's setup and usage flow.
- Improve the structure of the existing sections so the page reads like a real product guide instead of placeholder documentation.
- Keep the docs page aligned with the current dark app shell and navigation model.

## Capabilities

### New Capabilities
- `docs-content`: Improved documentation content and section structure for the `/docs` page.

### Modified Capabilities
None.

## Impact

- `src/pages/Docs.tsx` will be updated with clearer content and section organization.
- Supporting docs helper components or data files may be added under `src/pages/` if needed.
- No backend APIs or external dependencies are expected to change.
